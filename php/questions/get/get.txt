SELECT
  Q.ID,
  Q.userID,
  Q.askedDate,
  Q.text,
  COALESCE(SUM(A.ID),0) As totalAnswers
FROM
  Question Q
LEFT JOIN Answer A on A.questionID = Q.ID
GROUP BY
  Q.ID,
  Q.userID,
  Q.askedDate,
  Q.text,
  A.ID
